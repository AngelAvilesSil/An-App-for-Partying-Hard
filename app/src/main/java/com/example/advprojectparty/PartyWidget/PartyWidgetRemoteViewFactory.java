package com.example.advprojectparty.PartyWidget;

import android.content.Intent;
import android.widget.RemoteViewsService;

/*
public class PartyWidgetService extends RemoteViewsService {
    @Override
    public RemoteViewsFactory onGetViewFactory(Intent intent) {
        return new PartyRemoteViewsFactory(this.getApplicationContext(), intent);
    }
}

class PartyRemoteViewsFactory implements RemoteViewsService.RemoteViewsFactory {

    // See the RemoteViewsFactory API reference for the full list of methods to
    // implement.

}
*/
